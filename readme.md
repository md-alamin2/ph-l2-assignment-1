
# ১. TypeScript-এ `Interface` এবং `Type` এর মধ্যে পার্থক্য

যদিও `interface` এবং `type` প্রায়ই একই ভাবে ব্যবহার করা যায়, তবে তাদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে:

### ডিক্লারেশন মার্জিং (Declaration Merging)
- `Interface` ডিক্লারেশন মার্জিং সাপোর্ট করে (একই নামের একাধিক ডিক্লারেশন স্বয়ংক্রিয়ভাবে একত্রিত হয়)।
- `Type` নতুন প্রপার্টি যোগ করার জন্য পুনরায় লেখা যায় না।

```bash
interface User {
  name: string;
}
interface User {
  age: number;
}
// ফলাফল: { name: string; age: number; }

type User = { name: string; }
type User = { age: number; } // Error: Duplicate identifier
```

### এক্সটেন্ডিং/ইন্টারসেক্টিং
- Interface `extends` কীওয়ার্ড ব্যবহার করে এবং একাধিক interface এক্সটেন্ড করতে পারে
- Type একত্রিত করার জন্য intersection (`&`) ব্যবহার করে।

### কি কি প্রতিনিধিত্ব করতে পারে
- Type প্রিমিটিভ, ইউনিয়ন, টাপল এবং অন্যান্য যেকোনো টাইপ প্রতিনিধিত্ব করতে পারে।
- Interface মূলত অবজেক্ট শেপের জন্য (যদিও এটি ফাংশন সিগনেচারও প্রতিনিধিত্ব করতে পারে)।

```bash
type ID = string | number; //  Type দিয়ে ইউনিয়ন type ব্যাবহার করা যায়
interface ID = string | number; // error: interface দিয়ে ইউনিয়ন type ব্যাবহার করা যায় না

type Coordinates = [number, number]; // Type দিয়ে টাপল type ব্যাবহার করা যায়
```

### পারফরম্যান্স
জটিল পরিস্থিতিতে ক্যাশিংয়ের কারণে কম্পাইলারের জন্য Interface চেক করা কিছুটা দ্রুত হতে পারে।

### কোথায় ব্যবহৃত হয়?
অবজেক্ট শেপ এবং পাবলিক API-এর জন্য interface ব্যবহার করা হয় (এগুলো বেশি এক্সটেন্সিবল), এবং ইউনিয়ন, টাপল বা জটিল টাইপ ম্যানিপুলেশনের জন্য type ব্যবহার করুন।

---

# ২. TypeScript-এ `any`, `unknown`, এবং `never` টাইপের পার্থক্য

এই তিনটি TypeScript-এর টাইপ সিস্টেমে তিনটি ভিন্ন concepts প্রতিনিধিত্ব করে:

### `any` - টাইপ:
- কোনো টাইপ চেকিং ছাড়াই যেকোনো অপারেশন অনুমোদন করে।
- যেকোনো টাইপে অ্যাসাইন করা যায় এবং যেকোনো টাইপ এতে অ্যাসাইন করা যায়।
- মূলত সেই ভেরিয়েবলের জন্য TypeScript-এর টাইপ বন্ধ সেফটি  করে।

```bash
let value: any = 5;
value = "hello";
value.foo.bar; // কোনো এরর নেই, কিন্তু রানটাইমে ক্র্যাশ হতে পারে
```

### `unknown` - টাইপ:
- `unknown` - `any`-এর টাইপ-সেফ বিকল্প।
- যেকোনো মান প্রতিনিধিত্ব করে, কিন্তু ব্যবহার করার আগে টাইপ নির্ধারণ করতে হবে।
- প্রথমে টাইপ চেক না করে এর উপর অপারেশন করা যায় না।
- যেকোনো টাইপ `unknown`-এ অ্যাসাইন করা যায়, কিন্তু `unknown` শুধুমাত্র `any` বা `unknown`-এ অ্যাসাইন করা যায়।
- টাইপ জানা না থাকলে `any`-এর চেয়ে এটি পছন্দনীয়।

```bash
let value: unknown = "hello";
value.toUpperCase(); // Error: প্রথমে টাইপ চেক করতে হবে

if (typeof value === "string") {
  value.toUpperCase(); //  টাইপ গার্ডের পরে OK
}
```

### `never` - টাইপ:
- `never` - এমন টাইপ নির্দেশ করে যা কখনো ঘটে না।
- এমন ফাংশনের জন্য ব্যবহৃত হয় যা কখনো রিটার্ন করে না (এরর থ্রো করে বা অসীম লুপ)।
- টাইপ নির্ধারণে অসম্ভব অবস্থা নির্দেশ করে।
- `never` যেকোনো টাইপে অ্যাসাইন করা যায়, কিন্তু যেকোনো টাইপ `never`-এ অ্যাসাইন করা যায় না (`never` ছাড়া)।

```bash
function throwError(): never {
  throw new Error("Something went wrong");
}
```


## 👨‍💻 Developer

**Md. Al-amin**  
Email: [mdalamin22671@gmail.com](mdalamin22671@gmail.com)  
GitHub: [https://github.com/md-alamin2](https://github.com/md-alamin2)